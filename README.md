# randome number genorter cli

## install

### clone the repo first

```
git clone https://github.com/Senpai-10/ran.git &&
cd ran &&
```

### install the binary

$ `cargo install --path .`

### or

$ `make install`
